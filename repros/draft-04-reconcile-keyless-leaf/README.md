# draft-04-reconcile-keyless-leaf

2.0.0-beta.17: `reconcile()` updates but never notifies for `Date`/`Map`/`Set` leaf values

This standalone browser reproduction mirrors the reviewed example in
`issue-drafts/04-reconcile-keyless-leaf.md`. Open the preview and follow the on-screen action;
the result is reported as an explicit PASS/FAIL verdict.

Pinned to `solid-js@2.0.0-beta.17` and `@solidjs/web@2.0.0-beta.17`.
