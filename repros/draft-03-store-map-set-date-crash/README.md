# draft-03-store-map-set-date-crash

2.0.0-beta.17: `Map`/`Set`/`Date` values in a store crash on internal-slot access and on any method call inside a setter

This standalone browser reproduction mirrors the reviewed example in
`issue-drafts/03-store-map-set-date-crash.md`. Open the preview and follow the on-screen action;
the result is reported as an explicit PASS/FAIL verdict.

Pinned to `solid-js@2.0.0-beta.17` and `@solidjs/web@2.0.0-beta.17`.
