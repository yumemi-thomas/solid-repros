# draft-15-boundary-dispose-skips-sibling-queue

2.0.0-beta.17: an effect that disposes its own boundary skips a sibling boundary's queued effects for that flush

This standalone browser reproduction mirrors the reviewed example in
`issue-drafts/15-boundary-dispose-skips-sibling-queue.md`. Open the preview and follow the on-screen action;
the result is reported as an explicit PASS/FAIL verdict.

Pinned to `solid-js@2.0.0-beta.17` and `@solidjs/web@2.0.0-beta.17`.
