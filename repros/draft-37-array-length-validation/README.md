# draft-37-array-length-validation

2.0.0-beta.17 regression: store array `length` accepts invalid values and skips JavaScript coercion

This standalone browser reproduction mirrors the reviewed example in
`issue-drafts/37-array-length-validation.md`. Open the preview and follow the on-screen action;
the result is reported as an explicit PASS/FAIL verdict.

Pinned to `solid-js@2.0.0-beta.17` and `@solidjs/web@2.0.0-beta.17`.
