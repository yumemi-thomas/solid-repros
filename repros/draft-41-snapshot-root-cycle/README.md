# draft-41-snapshot-root-cycle

2.0.0-beta.17 regression: `snapshot()` breaks root self-cycle identity after a store write

This standalone browser reproduction mirrors the reviewed example in
`issue-drafts/41-snapshot-root-cycle.md`. Open the preview and follow the on-screen action;
the result is reported as an explicit PASS/FAIL verdict.

Pinned to `solid-js@2.0.0-beta.17` and `@solidjs/web@2.0.0-beta.17`.
